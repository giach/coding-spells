#include<stdio.h>
#include<stdlib.h>

typedef struct item {
    int value;
    int key;
}

item *hash_array[SIZE];

int hash_code(int key) {
    return key % SIZE;
}

void insert(int value, key) {
    int hash;
    hash_index = hash_code(key);

    if(
}  

int main() {

    return 0;
}
